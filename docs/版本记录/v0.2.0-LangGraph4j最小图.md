# v0.2.0 LangGraph4j 最小图

## 目标

跑通 LangGraph4j 最基础的图执行流程。

## 内容

- `state`：`SimpleState`
- `node`：`GreeterNode`、`ResponderNode`
- `edge`：`START -> greeter -> responder -> END`
- 启动时执行 graph 并打印状态

## 学习重点

- state 是如何承载共享数据的
- node 如何读取 state、返回更新
- edge 如何控制执行顺序
- graph 如何编译并运行
